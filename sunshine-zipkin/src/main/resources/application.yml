server:
  port: 9040
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: @pom.artifactId@
  datasource:
    username: root
    password: root
  profiles:
    active: dev
