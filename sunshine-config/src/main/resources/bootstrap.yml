spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/cpazstido/config-test.git
          username:
          password:
          search-paths: /*
          default-label: ${spring.profiles.active}
          basedir: git-config